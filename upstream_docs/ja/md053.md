# `MD053` - 未使用のリンク定義があります

タグ: `links`

エイリアス: `link-image-reference-definitions`

このルールは、参照リンクの定義（`[label]: URL`）が存在するが、ドキュメント内のどこからもそのラベルが参照されていない場合にトリガーされます。

根拠: 未使用の定義はドキュメントを不必要に肥大化させ、メンテナンスを困難にします。
