# `MD042` - 空のリンクを使用しないでください

タグ: `links`

エイリアス: `no-empty-links`

このルールは、リンク先（URL）が空のリンクがある場合にトリガーされます。

```markdown
[リンクテキスト]()
```

適切なURLを入力するか、プレースホルダー（`#`など）を使用してください。

```markdown
[リンクテキスト](https://example.com)
```

根拠: 空のリンクはユーザーをどこにも導かず、混乱を招きます。
