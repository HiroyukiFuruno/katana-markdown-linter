# `MD056` - 表の列数を揃えてください

タグ: `table`

エイリアス: `table-column-count`

このルールは、表（テーブル）の各行で列数が一致していない場合にトリガーされます。

```markdown
| A | B | C |
|---|---|---|
| 1 | 2 |
```

これを修正するには、不足しているセル（`|`）を追加して列数を揃えてください。

根拠: 列数が一致しないテーブルは、一部の環境で正しくレンダリングされなかったり、崩れて表示されたりすることがあります。
